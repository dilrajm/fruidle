EECS 1012 Project

Team Name: Fruidle

Team Members:
1. Vinuk Dahanak; ID #218114280
2. Dilraj Mukkar; ID #216787129
3. Mohammadparsa Hajihosseini


Team Problems & Changes from Wireframe / Pt.1

* We made each level harder. 
  - Before level 1, 2 and 3 had a sequence of 2, 3 and 5 fruits respectivly. 
  - Now its 3, 5 and 7. Longer fruit sequences to guess.
  - Found original to be too easy. 
  - Had to switch level 1 and 2 fruit color theme as a result to keep it consistent with the "how to play" example as a result. 
    Which showed green fruit with sequence of 3.

* We made the rules box on each level into a pop-up that can be accessed via the (?) button in the top right.
  - Less clutter one the game pages. 

* We made the fruit emojis into transparent background pixel art PNGs. 
  - More aesthetic.

* We added an enter button to lock in guesses. 
  - Had it in Function Requirements but forgot to add it in the Wireframe.


Additional Notes

* We have made a concerted effort to make this game 8 bit / 16 bit retro game pixel art themed. 
  - Via icons, background images, titles and font choices.   
  - Purely for aesthetics and user experience. 


Individual Contributions: 

Vinuk Dahanak:

* I entered promts to AI generate all background images, titles and icons on the website.
  - Continiusly promted until I got images I was satisfied with. 
  - Fed icons into a transparent background creator to make it look nicer. 
  - Fed background images into AI resizer to get apprpriate ratios to fit page until I was satisfied. 
  - Mostly used Google's Gemini for image generation with some Adobe Express for AI editing. 

* I coded most of the HTML.
  - Created the layout: Including putting the images and buttons in the HTML and typing up all the text. 
  - Made sure HTML tags had ID or classes so they can be identified later in JavaScript or CSS.
  - Decided that the page will be naviagate through Javascript, instead of multiple HTML documents. 
  - Layed out the structure for pop-ups.
  - Needed to put a little CSS to help with the early HTML. 
  - HTML was later edited, cleaned up and revised by other teamates.
  - Fixed up to 60 syntax issues in the HTML after checking with HTML validator.

* CSS
  - Styled the home and (?) and pop-up close button using CSS. 
  - Made minor adjustments to spacing and sizing throughout the page.
  - Styled both the win and lose pop ups. 

* Javascript
  - Coded to ensure refresh stays on current page instead of going back to homepage. 
  - Coded so navigation resets to top of page.  
  - Coded to prevent scrolling behind pop-up. 
  - Coded to ensure buttons work on refresh. 
  - Coded the win pop up and lose pop up on win and lose condition.
  - Coded the try again button. 
  - Coded the high score tracking functionality by session.  


Dilraj Mukkar:

*Coded the homepage, and levels button functionality to swtich between pages  
	- made it so that we dont have individual html files for each page but rather only 
	  one page, made it so that each page thats active is shown, rest are hidden. 

*? help button popup coded, along with css elements, blurred background.

*Restructured all of the HTML so that its divided into each section such as homepage, and levels, and modal controls with proper indentation and comments.

*Coded all of the games functionality except timer, winlose popup, and scroll lock.
	-responsible for core game engine, created a levels object that maps number of fruits, avalaible fruit images to each level
	-coded the delete button, which updates current game state, also prevented deleting when cells are empty
	-coded the enter button to check each row after color coded.
	-made a randomized secret key generator, this selects fruit images from level specific pools, alloing duplicated,
		with corrected length matching each array size
	-input system: implemented click based input for fruit buttons inside the next available grid, prevented out of bounds exceptions, tracking based on 2D array setup
	-Row validation: only allows enter when rows are full, compares guess vs secret key, painted rows with wordle logic
	-Two pass evaluation: first pass marks green and removes from pool, second pass marks yellow while preventing double counting duplicates
	-Grid rendering and tile painting; implemented paintRow() to update the grids dynamically, also ensured CSS visuals apply consistently and correctly. 
	-Button lock freezing: implemeneted lock buttons to disable all buttons when the win or loss condition is met
	-Button Enabling: coded to reset the grid availability after loading a new level. 

Mohammadparsa Hajihosseini:
HTML

structured the page layout after base foundation by Vinuk, using photoshop to change photos generated by Vinuk, created buttons and text.

Assigned IDs and classes to HTML elements for easy targeting by CSS and JavaScript.

Laid out pop-ups and modal structures for rules.

Cleaned up HTML for consistency and fixed syntax issues.

CSS (Added ~350 lines)

Styled main pages, including colors, layouts, grids, and structures.

adding box-sizing:border-box for proper spacing

using vh instead of px in all backgrounds for flexibility on bigger/smaller screens.

Adjusted grid dimensions adding cells to the html and adjusting sizes. 

aligned the timer with the grid using "display:grid: options and margins.

styled the homepage “How to Play” section making transparent backgrounds for icons and box.

Designed and sized buttons using "display:grid" twice(6 fruit buttons + enter button) using background image centering plus photoshop and proper rations for a nice polish, ensuring proper layout and user interaction effects using translateY and brightness.

Ensured home and help buttons had appropriate size and positioning using display:flex and flex-start.

adding containers to center texts and all items commands like justify content text align and align items.

Applied tile coloring for Wordle-style feedback (tile-correct, tile-present, tile-absent).

Added hover and click effects for level select and game buttons to improve user feedback.

Adjusted spacing, sizing, and layout across pages for a cohesive design using padding margins and grids.

JavaScript

Developed a timer function that persists across refresh and starts at 0:00 by default.

Ensured page navigation resets scroll to the top on load.

Prevented scrolling behind pop-ups to improve UX.


Collaboration / Support

Integrated layout, CSS, and timer functionality with the core game engine.

Prepared the structure for dynamic updates by JavaScript game logic.
Collaboration / Support

Worked closely with the team to integrate layout, CSS, and timer functionality with the core game engine.
Ensured the structure was ready for dynamic updates by JavaScript game logic.